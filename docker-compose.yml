version: "3.9"
services:
  web:
    build: .
    ports:
      - "4900:8000"
  
    # volumes:
    #   - .:/code
  redis:
    image: "redis:alpine"
    command: redis-server --requirepass hashing-bibliotech   
    ports: 
      - "6379:6379"    
