# Core
fastapi==0.115.6
uvicorn[standard]==0.32.1

# Hot reload that actually works under virtiofs
watchfiles==1.0.3

# Type safety — assuming you're using these given the project description
pydantic==2.10.4

# HTTP client (useful for inter-service calls, testing)
httpx==0.28.1

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
